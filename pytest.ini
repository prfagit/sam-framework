[pytest]
# Keep tests quiet by default, skip performance tests, and stop on first failure for quick feedback.
addopts = -q -k 'not performance' --maxfail=1
asyncio_mode = auto
markers =
    performance: marks tests as performance tests

